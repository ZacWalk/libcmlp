*.csv filter=lfs diff=lfs merge=lfs -text

*.sh text eol=lf
*.ps1 text eol=crlf

# The vendored upstream runtime, including templates, is fingerprinted byte-for-byte.
/dd.ps1 -text whitespace=cr-at-eol
/.dd/** -text whitespace=cr-at-eol
